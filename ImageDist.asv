function [W] = ImageDist(A)
  [h, w, ~] = size(A);
  
  n = h*w;
  
  B = reshape(A, [] ,3);
  
  for i = 1 : h:
      for j = 1 : w:
          
  
  
  
  W = zeros(n);
 
  counter = 1;
  for i = 1 : n
    for j = 1 : n
      W(i,j) = exp(-0.5 * norm(A(:,i)-A(:,j), 2)^2 / sigma^2);
      W(j,i) = W(i,j);
    end
  end

end
  